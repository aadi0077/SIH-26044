name: sih-26044

services:
  postgres:
    image: postgres:16-alpine
    container_name: sih-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: aadi88
      POSTGRES_DB: sih_db
    ports:
      - "5432:5432"
    volumes:
      - sih-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d sih_db"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  sih-pgdata:
